mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.panacealab.panacare.entity

logging:
  level:
    com.panacealab.panacare.dao: debug

